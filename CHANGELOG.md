# Historial de cambios

| Fecha | Qué cambió | Por qué |
|---|---|---|
| 2026-09-13 | Estructura inicial del repositorio: `README.md`, `DECISIONES.md`, `CHANGELOG.md`, `.gitignore`, `marca/contexto.md`, `marca/design-system.md`, `marca/tokens.css` y `piezas/carruseles/`. Los dos documentos de origen (`SALUHACKERS_Brand_Reference.md` y `contexto-maestro.md`) salen del árbol de git. | Separar la marca, la voz y el diseño (este repositorio) de los datos de negocio (Notion), y dejar un contexto limpio y estable que puedan consumir personas y agentes. |
| 2026-09-13 | Nueva `marca/voz.md` (versión 1.0): reglas duras, tono y voz, léxico prohibido, frases ancla, quién firma qué y ejemplos. Las secciones trasladadas se retiran de `marca/design-system.md` (§3, voz) y de `marca/contexto.md` (Tono y voz) y se deja en su lugar una línea de referencia. | La voz vivía en tres sitios y empezaba a divergir. Un único archivo evita contradicciones y da a personas y agentes una sola fuente. |
| 2026-09-13 | Nuevo `DESIGN.md` en la raíz siguiendo el estándar DESIGN.md de Google (tokens en YAML más prosa; 0 errores en su validador). `marca/tokens.css` pasa a ser CSS válido por sí solo: el bloque del grano se envuelve en `.sh-grain{}` sin tocar ningún valor. Se retira `marca/design-system.md` (su contenido vive en `DESIGN.md` y `tokens.css`; el original sigue en `_origen/`). README con la regla «tokens.css manda» y el comando de validación. | Un solo documento visual, en un formato que agentes y validador entienden, y sin duplicados que diverjan. |
